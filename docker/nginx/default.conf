server {
    listen 80;
    server_name localhost;

    absolute_redirect off; 

    location /prge_kw {
        alias /usr/share/nginx/html/prge_kw;
        try_files $uri $uri/ /prge_kw/index.html;
    }
}